# STM32H743 控制板 · 串口烧录方案

> **目标**：摆脱烧录器，用电脑 USB 转串口直接烧录固件。
> **板型**：HZ2.869.1300DL 控制板（STM32H743 + 8 路全双工 RS-422）
> **版本**：v3 · 2026-09-15
> **v1 更正**：v1 把 J4 写成"UART4、RS-422"，**是错的**（UART4 在 J10）。J4 实为 SWD 下载口。详见 §3。
> **v3 更新**：实物照片确认 MCU 实为 **STM32H743VIT6（2MB）**、J4 丝印为 `3.3V/SWDIO/SWCLK/GND`。两项待确认已收敛，详见 §9。

---

## 一、结论

**用 USART1（PA9/PA10）→ J9（通道 5）。**

它是全板唯一同时满足三个条件的选择：
1. 代码里完全没被占用
2. H743 的 ROM Bootloader 支持
3. 物理上是个空通道，不用拔任何外设

---

## 二、推导过程（三步）

### 第 1 步：代码里谁在忙？

工程应用层源码只有 `App/`、`Bsp/UART/`、`Bsp/LED/` 三个模块（无其他隐藏源文件）。逐条核对：

| 位置 | 事实 |
|---|---|
| `App/main.c:67-70` | 只调用了 `uart2_init` / `uart3_init` / `uart4_init` / `uart7_init`，全部 921600 |
| `App/main.c:48-55` | `fputc` 把 printf 直接绑到 **USART3** |
| `App/main.c:130` | 主循环每 1ms 用 `uart2_transmit` 发 20 字节（0xAA 帧头 + 校验和） |
| `Bsp/UART/bsp_uart.c:154` | `uart1_init()` 实现完整 |
| 全工程 | **`uart1_init` 零调用点** |

| UART | 代码状态 |
|---|---|
| **USART1** | **零占用**（BSP 已现成，只差一次调用） |
| USART2 | 已初始化 + 每 1ms 有业务数据 |
| USART3 | 已初始化 + printf 占用 |
| UART4 | 已初始化（暂无数据收发） |
| UART7 | 已初始化（暂无数据收发） |
| USART6 / UART5 / UART8 | 未初始化 |

### 第 2 步：图纸上谁通到哪里？

> 依据 `tmp/pdfs/source/`（**原始渲染图，未加中文标注的那版**），第 1、2 页。

| 接插件 | 通道 | 隔离域 | UART | MCU 引脚 | 隔离器 | 收发器 |
|---|---|---|---|---|---|---|
| J5 | 1 | ISO1 | USART2 | PA2 / PA3 | U4 | U11 |
| J6 | 2 | ISO1 | UART7 | PE8 / PE7 | U5 | U12 |
| J7 | 3 | ISO1 | USART3 | PB10 / PB11 | U6 | U13 |
| J8 | 4 | ISO2 | USART6 | PC6 / PC7 | U8 | U14 |
| **J9** | **5** | **ISO2** | **USART1** | **PA9 / PA10** | **U9** | **U15** |
| J10 | 6 | ISO2 | UART4 | PC10 / PC11 | U10 | U16 |
| J2 | 7 | 主地 | UART5 | PC12 / PD2 | — | U2 |
| J3 | 8 | 主地 | UART8 | PE1 / PE0 | — | U3 |

- 隔离器型号：**NSI8021N1-DSPR**
- 收发器型号：**MAX3490EESA-JSM**（全双工）

> **实物与图纸不符的一处（重要）**：图纸上 U1 标注为 `STM32H743VGT6`，但**实物芯片丝印是 `STM32H743VIT6`**（照片可辨）。
> VGT6 = 1MB，VIT6 = **2MB**。实物与工程目录名 `stm32h743vit6`、`main.c:279` 里"Flash 2MB"的 MPU 配置三者一致。
> **结论：以实物为准，芯片是 2MB 的 VIT6，图纸标注是笔误。** 这意味着 `.icf` 里 FLASH2 区域（`0x08100000`）**有效可用**。

其余接口：J1 = SPI1、J12 = SPI2、**J4 = SWD 下载口**、J11 = 12V 主电源输入。
I2C1（PB6/PB7）挂 U22 BL24C32F（EEPROM）+ U23 STS40（温度）；PB8 = 24LC_WP。Y2 = 25MHz 晶振。

### 第 3 步：Bootloader 认谁？

STM32H743 的系统 Bootloader 固化在 ROM（擦不掉、写不坏），进入条件是**复位瞬间 BOOT0 = 1**。它只在这三个串口上监听：

| Bootloader 串口 | 引脚 | 本板出口 |
|---|---|---|
| **USART1** | PA9 / PA10 | **J9（通道 5）** |
| USART2 | PA2 / PA3 | J5（通道 1） |
| USART3 | PB10 / PB11 | J7（通道 3） |

UART4 / 5 / 6 / 7 / 8 **一律不支持**，这条路走不通。

### 三条件交集

| 串口 | 代码空闲 | Bootloader 支持 | 是空通道 | 结论 |
|---|---|---|---|---|
| **USART1 / J9** | ✅ | ✅ | ✅ | **唯一解** |
| USART2 / J5 | ❌ 每 1ms 发数据 | ✅ | ❌ GSA200 占用 | 需拔传感器 |
| USART3 / J7 | ❌ printf 占用 | ✅ | ❌ 调试口 | 会与打印抢线 |
| USART5 / J2 | ✅ | ❌ | ✅ | Bootloader 不认 |
| USART6 / J8 | ✅ | ❌ | ✅ | Bootloader 不认 |
| USART8 / J3 | ✅ | ❌ | ✅ | Bootloader 不认 |

**USART1 是唯一"插上就能用、不用拔线、不用改业务"的口。**

---

## 三、J4 是什么（v1 错误更正）

v1 写"J4 = UART4、RS-422 差分" —— **错**。UART4 在 J10。

原始图纸第 1 页明确：**J4 = 6 脚座（ZJWF-PH08-4P01），标注"单片机下载接口"**

| J4 针脚 | 接到 |
|---|---|
| 1 | VCC3.3V |
| 2 | SWDIO（经 R18 10k） |
| 3 | SWCLK（经 R19 10k） |
| 4 | GND |
| 5、6 | 空焊盘，未连接 |

> **实物已核对**：板上 J4 旁边的 PCB 丝印就是四行字 `GND` / `SWCLK` / `SWDIO` / `3.3V`，与上表完全一致。

**J4 走的是 SWD，不是串口。**

- 烧录器（ST-Link / J-Link）→ 接 J4 ✅
- USB 转串口模块 → 接 J4 ❌ **完全没有反应**（SWD 和 UART 是两套完全不同的协议）

> 附带更正：`tmp/pdfs/annotated_schematic.html` 里"J4 另引出 UART1_TX/RX"这句也是错的 —— 图上 5、6 脚是空焊盘，且 J4 附近根本没有 UART1 的网络名。

---

## 四、J9 的电气真相（决定买什么模块）

### 链路

```
MCU PA9 (68)  ─ UART1_TXD ─┐
                            ├─ U9  NSI8021N1-DSPR（数字隔离）
MCU PA10 (69) ─ UART1_RXD ─┘         │
                                     ▼
                     U15 MAX3490EESA-JSM（全双工 RS-422 收发器）
                                     │
                                     ▼
                     J9（ZJWF-PH08-4P01）+ R17/R20 = 120Ω 终端
```

### J9 针序

| 针 | 网络 | 含义 |
|---|---|---|
| 1 | RS422_5_Y | 板端发送 +（TX+） |
| 2 | RS422_5_Z | 板端发送 −（TX−） |
| 3 | RS422_5_B | 板端接收 −（RX−） |
| 4 | RS422_5_A | 板端接收 +（RX+） |

### ⚠️ 三条必须先知道的

**1. J9 不是 TTL 串口。**
出来的是差分信号，中间隔着 MAX3490 收发器。**普通 USB 转 TTL 模块（CH340 / CP2102 那种）接 J9 不会通。**

- 必须买 **USB 转 RS422 四线全双工**模块（T+ / T− / R+ / R− 四个端子）
- 别买 2 线 RS485 半双工：J9 是**四线全双工**（MAX3490 驱动器常开，没有 DE 方向脚），2 线模块只能接一半

**2. MAX3490 驱动器常开。**
没有方向控制脚，板子会一直往 J9 的 Y/Z 上驱动信号 —— 所以 J9 上不要再并接别的从机。

**3. 隔离侧靠板上电源供电。**
供电链（图纸第 3 页）：

```
J11（CON4, +12V）→ U17 TPS54331DDAR → +5V
                   ├─ U7  DS8242-33S3 → VCC3.3V（主 3.3V）
                   ├─ U18 B0505S-1WR3 → U20 DS8242-33S3 → +3.3V_ISO1
                   └─ U19 B0505S-1WR3 → U21 DS8242-33S3 → +3.3V_ISO2
```

**不给 J11 送 12V，J9 就是死的。** 注意这是**独立于固件**的：B0505S 直接从 +5V 取电，MCU 处于 Bootloader 还是跑应用都不影响。但每次烧录前必须上电。

### 接线表

| USB-RS422 模块 | J9 针脚 | 方向 |
|---|---|---|
| R+ | 1（Y） | 板 TX+ → 电脑 |
| R− | 2（Z） | 板 TX− → 电脑 |
| T− | 3（B） | 电脑 → 板 RX− |
| T+ | 4（A） | 电脑 → 板 RX+ |

> J9 **没有 GND 针脚**。若通信不稳定，把模块 GND 接到 J9 附近的隔离侧地（GND2）。这会短掉隔离，台面调试可以接受。

### 如果手上只有 USB 转 TTL 模块

板上 8 个 UART **全部**经 RS-422 收发器，**没有任何 TTL 串口**。想用 TTL 模块只能动烙铁：

- **推荐**：焊到 U9 的 MCU 侧（第 2、3 脚 = UART1_TXD / UART1_RXD），这里是 3.3V TTL
- 不推荐：焊 MCU 的 PA9/PA10 —— LQFP100 是 0.5mm 间距，难度大得多

---

## 五、两个绕不开的硬约束

### 5.1 BOOT0 被硬下拉 → 进 Bootloader 只能靠软件跳转

图纸事实：

- `BOOT0（pin 94）— R10 10k — GND`
- `PB2/BOOT1（pin 36）— R1 10k — GND`
- **无跳线、无测试点**

**含义**：没法像常见开发板那样用跳线帽进 Bootloader。唯一常规通道是：固件收到特定指令后，主动跳进系统存储器。

**代价**：跳转函数本身是单点故障 —— 它要是坏了，就只能回到 J4 上的烧录器。所以 §6 的 Phase 2 现场验证是**硬性闸门**，不能省。

**要不要根治**：可以拆掉 R10，在 BOOT0 上飞一根线接开关到 3.3V。这样复位时拉高就能硬进 Bootloader，彻底摆脱对固件的依赖。需要 SMD 返修。

### 5.2 当前工程是 RAM 版，不能直接烧进 Flash

`Project/EWARM/servo_app.icf` 实测：

| 行 | 内容 | 影响 |
|---|---|---|
| `:5` | `intvec_start = 0x24000000` | 向量表在 AXI SRAM |
| `:77` | `//place in FLASH_region { readonly };` | **被注释掉了** |
| `:78` | `place in AXISR_region { readonly };` | **代码段全放 AXI SRAM** |
| `:75` | `place at address mem:0x24000000 { readonly section .intvec }` | 配合上面 |

配合 `main.c:144` 的 `SCB->VTOR = &__vector_table[0]`，**整个程序都跑在 RAM 里**。

**含义：现在板子脱离烧录器根本起不来。** Flash 里没有有效镜像，冷启动时 CPU 从 `0x08000000` 读到的 MSP 是 `0xFFFFFFFF`，直接 HardFault。

所以"用串口烧录"的前提是：**必须先做一个 Flash 版链接配置。**

要点：

- 向量表放 **`0x08000000`**
- ⚠️ **不要照抄** `Driver/iar将程序在ram和flash中切换.txt` 里的 `0x08020000` —— 那是"Bootloader 占前 128K + 应用放后面"的两段式布局。现在是**单应用**，用 `0x08020000` 会冷启动跑飞
- `SCB->VTOR = &__vector_table[0]` **不用改** —— 它跟着链接脚本走，比手写死地址更稳

---

## 六、实施步骤

### Phase 0 — 备料与核对（不动物件）

| # | 事项 | 为什么 |
|---|---|---|
| 0.1 | 买 USB 转 RS422 **四线全双工**模块 | J9 是差分，TTL 模块用不了 |
| 0.2 | 装 STM32CubeProgrammer（GUI + CLI 都要） | CLI 用于脚本化 |
| 0.3 | ~~核对 MCU 丝印 VG/VI~~ → **已完成：实为 VIT6（2MB）** | 见 §2 第 2 步的说明。`.icf` 的 FLASH2 区域有效 |
| 0.4 | 用烧录器读 `0x1FF00000` 开头的内存 | 确认 Bootloader 真实入口，见 §8-1 |

### Phase 1 — 固件改造

| # | 事项 | 说明 |
|---|---|---|
| 1.1 | 新建 Flash 版 .icf | intvec @ `0x08000000`，`place in FLASH_region { readonly }` |
| 1.2 | 与现有 RAM 版 .icf 分开存放 | 防烧错版本 |
| 1.3 | main 里补 `uart1_init(115200)` | BSP 已现成，只差调用 |
| 1.4 | 加跳转函数 | ⚠️ **执行顺序见 §8-2，v1 的顺序是错的** |
| 1.5 | 加触发条件 | 见 §8-3 |
| 1.6 | IAR Output Converter → Intel HEX | 产出 `servo_app.hex` |

### Phase 2 — 一次性过渡（关键闸门，不可跳过）

1. 烧录器接 **J4**，把 Flash 版固件烧进去 —— **这是最后一次用烧录器**
2. **断电重启，先确认板子能独立运行**（LED 闪）
   - 这一步单独验证「Flash 镜像 + VTOR + .icf」是否正确
   - **必须先过这关再验跳转** —— 否则出问题分不清是哪个环节
3. 上电 → J9 接 USB-RS422 → 触发跳转 → CubeProgrammer 连接
4. 能读回 Bootloader 版本号（正确回 `0x79`）= 通过
5. **只有第 4 步通过，烧录器才可以退役。** 不通过就别退 —— 跳转函数是单点故障

> 建议：验证期间烧录器一直插在 J4 别拔，出问题能立刻回滚。

### Phase 3 — 日常烧录流程

```
① J11 上电（必须，否则隔离侧没电）
② J9 插 USB-RS422（可常驻，无业务冲突）
③ 发触发帧 → 固件跳到 Bootloader
④ STM32_Programmer_CLI 烧录（8E1 偶校验）
⑤ 自动复位，从 Flash 正常运行
```

建议把 ③④ 打包成一键脚本。

### Phase 4 — 可选增强

- **BOOT0 硬件改造** —— 彻底摆脱软跳转依赖（见 §5.1）

  > **为什么这个值得做（不只是"可选"）**：
  > 软跳转有个本质弱点 —— **它依赖当前固件是"活着"的**。若烧进去一版卡在早期初始化（比如 UART1 还没起来）的固件，就永远触发不了跳转，只能又翻出 ST-Link。
  > 而做这套东西的初衷恰恰是不想再用 ST-Link。
  >
  > BOOT0 可控制之后：入口地址问题消失、跳转代码整个可以删掉、固件失控时照样能烧录。
  >
  > **做法**：R10 保留（提供默认下拉），从 R10 靠 BOOT0 一侧的焊盘引线出来，串开关接 3.3V。断开 = 正常启动，闭合 = 进 Bootloader。
  > **顺序**：先把 Phase 1~3 跑通再动烙铁 —— 万一改坏了 R10 焊盘，软跳转这条路也没了，就真的只剩 J4。
- **自定义 IAP + 双 Bank 回滚** —— 可用任意 UART
  - ⚠️ 前提：`main.c:279` 的 MPU 把 Flash 配成了 **PRIV_RO_URO（只读）**。自己写 Flash 之前必须先改这个区域或关掉 MPU，否则会当场 MemManage Fault

---

## 七、风险清单

| # | 风险 | 应对 |
|---|---|---|
| 1 | **Bootloader 入口地址存疑** —— v1 写的 `0x1FF00000`，查 AN2606 的 H74x/75x 条目列的是 `0x1FF1E7FE`（非 4 字节对齐，更像版本记录位置）。两者不一致 | Phase 0.4 读内存确认 + Phase 2 现场验证，见 §8-1 |
| 2 | **跳转函数时钟回退顺序错误** —— v1 的写法会当场死机 | 见 §8-2 |
| 3 | **校验位切换** —— 触发阶段 8N1，Bootloader 握手阶段 8E1 | 脚本里两步显式分开配置 |
| 4 | **`0x08020000` 陷阱** | 单应用必须用 `0x08000000` |
| 5 | ~~VG/VI 丝印不符~~ **已排除** | 实物确认为 VIT6（2MB） |
| 6 | **J9 无 GND** | 异常时接隔离侧 GND2 |
| 7 | **跳转函数单点故障** | Phase 2 现场验证；BOOT0 飞线兜底 |
| 8 | **PB14 冲突** —— 本板 PB14（pin 53）= SPI2_MISO，通到 J12 | AN2606 的 H743 表里没有"USART1 TX 镜像到 PB14"这条，存疑，优先级低。若 J12 接了从机，烧录时留意 |
| 9 | **RAM/Flash 两版 .icf 混用** | 分目录存放，命名区分 |

---

## 八、三个必须想清楚的技术细节

### 1. Bootloader 入口地址

STM32H743 的系统存储器在 `0x1FF00000`（128KB）。跳转需要从入口处读两个字：第 1 个是 MSP 初值，第 2 个是复位向量。

**现有信息矛盾**：

- 常见做法（也是 v1 写的）：跳到 `0x1FF00000`
- 查 AN2606 的 H74x/75x 条目：Bootloader 位置标的是 `0x1FF1E7FE` —— 这个地址**不是 4 字节对齐，不可能是向量表**，更像版本/ID 的记录位置

**怎么确认**（Phase 0.4，趁烧录器还在）：

用烧录器挂上去，读 `0x1FF00000` 开头 8 个字节：

- 若第 1 个字是合理的栈指针（落在 RAM 范围，如 `0x2000xxxx` / `0x2400xxxx`）
- 且第 2 个字指向 `0x1FF0xxxx` 区间（Bootloader 自己的代码）

→ 说明 `0x1FF00000` 就是正确入口。

若读出来是 `0xFFFFFFFF` 或明显不合理，就要去 AN2606 查这颗芯片的准确入口再改。

**保险做法**：跳转前先校验这两个字是否合理，不合理就不跳（避免跳到空地址把板子跑死）。

### 2. 跳转函数的执行顺序（v1 这里是错的）

v1 的写法顺序：

```
关中断 → 关 SysTick → 关 Cache → 关 HSE → 关 PLL1 → 切 SYSCLK 到 HSI → 等待 → 跳转
                                     ↑
                               问题出在这里
```

**问题**：当前 SYSCLK 就是从 PLL1（经 HSE 25MHz）来的。先关 HSE / 关 PLL1，等于把 CPU 正在用的时钟掐了 —— 后面的代码根本没机会执行。

**正确顺序**：

```
1. 关中断（__disable_irq）
2. 关 SysTick
3. 先把 SYSCLK 切到 HSI，并等待切换完成后才往下走    ← 关键
4. 确认已经跑在 HSI 上之后，再关 PLL1/PLL2/PLL3、关 HSE
5. 关 D-Cache / I-Cache
6. 关 MPU（可选但推荐）
7. 设置 MSP = 入口第 1 个字，跳转到入口第 2 个字
```

**关于 MPU**：查了 `main.c:273-343` 的配置 —— 它用 `LL_MPU_Enable(LL_MPU_CTRL_PRIVILEGED_DEFAULT)`，即 PRIVDEFENA = 1。未覆盖区域回退到默认内存映射，而 `0x1FF00000` 在默认映射里属于可执行可访问的 Code 区，**所以 MPU 不会挡住跳转**。但为了干净，跳之前关掉更稳妥。

**关于 Flash 等待周期**：切到 HSI（64MHz）后，Flash 延迟如果还是按 480MHz 配的，不影响跳转（只是慢一点）。切过去就跳走，不用管。

### 3. 触发方式

推荐：**连续收到 3 个 `0x7F` 就跳转**。

为什么用 `0x7F`：

- 它是 Bootloader 握手的标准起始字节，PC 端工具连接时本来就会发
- 所以同一串字节既能触发跳转，也能完成 Bootloader 自身的自动波特率检测 —— 一步到位
- USART1 是空通道，没有业务数据流，**不存在误触发风险**

`uart1_rx` 的 DMA 接收在 `bsp_uart.c` 里已经实现，直接挂解析即可。

⚠️ 波特率/校验切换：发触发帧时用 8N1（和 `uart1_init` 一致），Bootloader 握手阶段要切到 8E1。

### 4. Flash 等待周期与电压档位（**已验证，非风险**）

改成 Flash 版后，每条指令取指都要经过 Flash，所以等待周期变得重要。核对结论：

**权威来源**：`Driver/Src/stm32h7xx_ll_utils.c` 里的 `LL_SetFlashLatency()` 把 ST 的等待周期表硬编码了。该文件按器件分三套表，**H743 对应的是 `STM32H7_DEV_ID == 0x450UL` 那一套**（依据 `STM32H7xx_Device/Inc/stm32h743xx.h:21217`）。

| HCLK | ST 要求的等待周期 |
|---|---|
| ≤ 70 MHz | 0 WS |
| 70 < HCLK ≤ 140 MHz | 1 WS |
| **140 < HCLK ≤ 240 MHz** | **2 WS** |

**本工程实际情况**：HCLK = 480 ÷ 2（`main.c:235`）= **240 MHz**，需要 2 WS；而 `main.c:197` 设的是 **4 WS**。

**4 > 2，比要求更保守 → 安全。** 等待周期给多了只是 Flash 读得慢，给少了才会读错数据。

同时确认：**HCLK 240MHz 在 VOS1 下是允许的**（表里 VOS1 上限就是 240MHz），且 0x450 的表里没有 SCALE0 行，说明 VOS1 即最高档，与 `main.c:179` 的 `SCALE1` 一致。

> ⚠️ 排查时注意：`stm32h7xx_ll_utils.c` 里 0x480 / 0x450 / 0x483 三套表数值差很多，**看错分支会得出相反结论**（0x480 那套会算出"240MHz 需 5WS"，从而误判为危险）。

**结论**：这条不是风险。改成 Flash 版后剩下的真实差异只有两条 —— ① `initialize by copy` 的初值必须在 Flash 里（正确性问题，见 Phase 2 验证）；② 从 Flash 取指比 RAM 慢（性能差异，对本工程无影响）。

### 5. 波特率为什么选 115200

`main.c` 里**没有**设置 USART 时钟源，走的是 H7 复位默认值 —— USART1 挂在 **PCLK2** 上。按时钟树算：
SYSCLK 480MHz → AHB ÷2 = 240MHz → APB2 ÷16 = **15MHz**。

| 目标波特率 | 实际可达到 | 误差 | 评价 |
|---|---|---|---|
| **115200** | 115385 | **0.16%** | ✅ 稳 |
| 921600 | 937500 | **1.72%** | ⚠️ 临界，叠加转换器误差容易握手失败 |

**烧录用 115200。**

> 顺带一提：现有业务口（`main.c:67-70`）也跑在 921600，也就是 1.72% 误差下工作。如果 J5/J6/J7/J10 上偶尔出现丢字节，波特率误差是个值得怀疑的方向。

---

## 九、待确认清单

- [x] ~~MCU 丝印 VG 还是 VI~~ → **已确认：STM32H743VIT6（2MB）**，`.icf` 的 FLASH2 区域有效
- [x] ~~J4 是否真是 SWD~~ → **已确认**，板上丝印即 `3.3V / SWDIO / SWCLK / GND`
- [ ] **Bootloader 真实入口地址** ← 现在最重要的一条，Phase 0.4 用烧录器读内存确认
- [ ] J4 的 5、6 脚实物是否真的空焊（图纸上是空焊盘，照片分辨率看不出）
- [ ] 实物丝印 J5~J10 是否与图纸一致

---

## 十、参考来源

- AN2606 系统存储器启动模式（ST 官方）
- STM32H743/753xx Bootloader 通信外设表（AN2606 Table 2）
- 图纸原始渲染：`tmp/pdfs/source/source-page-1.png`、`source-page-2.png`
- 带中文标注版：`output/pdf/控制板原理图_中文标注分析.pdf`
  （注：该标注版中 J4 的描述、"J4 另引出 UART1_TX/RX"均有误，以本文档为准）
